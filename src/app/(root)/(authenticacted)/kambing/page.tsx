"use client"

import { useModal } from "@/hooks/use-modal-store";

type Props = {}

const Page = (props: Props) => {
    return (
        <div>
            page
        </div>
    )
}

export default Page